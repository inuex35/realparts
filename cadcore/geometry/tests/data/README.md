# cadcore/geometry/tests/data

STEP files produced by other CAD systems, used by `test_imported.py` to
check that imported geometry gets stable face names and survives the same
operations as native geometry.

| file | contents |
|---|---|
| `foreign.step` | a single part from another kernel |
| `many_shapes.step` | a file containing several solids |
