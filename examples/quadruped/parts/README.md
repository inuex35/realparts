# examples/quadruped/parts

Parts of the quadruped assembly in `../robot.json`.

| file | what it is |
|---|---|
| `body.json` | the chassis: a plate with a hip pin at each corner, and the neck, head, muzzle and ears on the front |

The legs are built from parameters inside `robot.json` itself rather than
from separate files.
